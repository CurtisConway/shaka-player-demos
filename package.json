{
  "name": "shaka-player-demos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf docs/*.png;rm -rf docs/*.js;rm -rf docs/*.css;rm -rf docs/*.html;rm -rf docs/*.map",
    "watch": "parcel index.html --out-dir docs --log-level 4 --no-cache",
    "build": "npm run prebuild; parcel build index.html --out-dir docs --log-level 4 --no-cache"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CurtisConway/shaka-player-demos.git"
  },
  "author": "Curtis Conway",
  "license": "MIT",
  "dependencies": {
    "mux.js": "^5.1.3",
    "shaka-player": "^2.5.4"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
